function calcObservations() {
	let mode = sessionStorage.getItem("patientmode");
	let weight = sessionStorage.getItem("patientweight");
	let geriatric = sessionStorage.getItem("patientgeriatric");

		// Observations determinant
	if (weight >= 0 && weight <= 5) {
		var HRlow = 100,
			HRhi = 160,
			RRlow = 30,
			RRhi = 50,
			SBPlow = 75,
			SBPhi = 100,
			DBPlow = 50,
			DBPhi = 70,
			DefibJ = (weight * 4),
			CBVLo = (weight * 80),
			CBVHi = (weight * 100),
			LMA = 1
	} else if (weight > 5 && weight <= 9) {
		var HRlow = 80,
			HRhi = 140,
			RRlow = 20,
			RRhi = 30,
			SBPlow = 75,
			SBPhi = 100,
			DBPlow = 50,
			DBPhi = 70,
			DefibJ = (weight * 4),
			CBVLo = (weight * 70),
			CBVHi = (weight * 80),
			LMA = 1.5
	} else if (weight > 9 && weight <= 11) {
		var HRlow = 80,
			HRhi = 130,
			RRlow = 20,
			RRhi = 30,
			SBPlow = 80,
			SBPhi = 110,
			DBPlow = 50,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2
	} else if (weight > 11 && weight <= 13) {
		var HRlow = 80,
			HRhi = 130,
			RRlow = 20,
			RRhi = 30,
			SBPlow = 80,
			SBPhi = 110,
			DBPlow = 50,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2
	} else if (weight > 13 && weight <= 15) {
		var HRlow = 80,
			HRhi = 130,
			RRlow = 20,
			RRhi = 30,
			SBPlow = 80,
			SBPhi = 110,
			DBPlow = 50,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2
	} else if (weight > 15 && weight <= 17) {
		var HRlow = 80,
			HRhi = 120,
			RRlow = 20,
			RRhi = 30,
			SBPlow = 80,
			SBPhi = 110,
			DBPlow = 50,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2
	} else if (weight > 17 && weight <= 19) {
		var HRlow = 80,
			HRhi = 120,
			RRlow = 20,
			RRhi = 30,
			SBPlow = 80,
			SBPhi = 110,
			DBPlow = 50,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2
	} else if (weight > 19 && weight <= 21) {
		var HRlow = 70,
			HRhi = 110,
			RRlow = 15,
			RRhi = 30,
			SBPlow = 85,
			SBPhi = 120,
			DBPlow = 55,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2
	} else if (weight > 21 && weight <= 23.5) {
		var HRlow = 70,
			HRhi = 110,
			RRlow = 15,
			RRhi = 30,
			SBPlow = 85,
			SBPhi = 120,
			DBPlow = 55,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2
	} else if (weight > 23.5 && weight <= 25) {
		var HRlow = 70,
			HRhi = 110,
			RRlow = 15,
			RRhi = 30,
			SBPlow = 85,
			SBPhi = 120,
			DBPlow = 55,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2
	} else if (weight > 25 && weight <= 26.5) {
		var HRlow = 70,
			HRhi = 110,
			RRlow = 15,
			RRhi = 30,
			SBPlow = 85,
			SBPhi = 120,
			DBPlow = 55,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2.5
	} else if (weight > 26.5 && weight <= 30) {
		var HRlow = 70,
			HRhi = 110,
			RRlow = 15,
			RRhi = 30,
			SBPlow = 85,
			SBPhi = 120,
			DBPlow = 55,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 2.5
	} else if (weight > 30 && weight <= 34) {
		var HRlow = 70,
			HRhi = 110,
			RRlow = 15,
			RRhi = 30,
			SBPlow = 85,
			SBPhi = 120,
			DBPlow = 55,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 3
	} else if (weight > 34 && weight <= 38) {
		var HRlow = 60,
			HRhi = 105,
			RRlow = 15,
			RRhi = 20,
			SBPlow = 85,
			SBPhi = 120,
			DBPlow = 55,
			DBPhi = 80,
			DefibJ = (weight * 4),
			CBVLo = (weight * 65),
			CBVHi = (weight * 75),
			LMA = 3
	} else if (weight > 38 && weight <= 49) {
		var HRlow = 60,
			HRhi = 100,
			RRlow = 12,
			RRhi = 20,
			SBPlow = 80,
			SBPhi = 120,
			DBPlow = 60,
			DBPhi = 80,
			DefibJ = 200,
			CBVLo = (weight * 65),
			CBVHi = (weight * 70),
			LMA = 3
	} else if (weight > 49 && weight <= 69) {
		var HRlow = 60,
			HRhi = 100,
			RRlow = 12,
			RRhi = 20,
			SBPlow = 80,
			SBPhi = 120,
			DBPlow = 60,
			DBPhi = 80,
			DefibJ = 200,
			CBVLo = (weight * 65),
			CBVHi = (weight * 70),
			LMA = 4
	} else if (weight > 69 && weight <= 99) {
		var HRlow = 60,
			HRhi = 100,
			RRlow = 12,
			RRhi = 20,
			SBPlow = 80,
			SBPhi = 120,
			DBPlow = 60,
			DBPhi = 80,
			DefibJ = 200,
			CBVLo = (weight * 65),
			CBVHi = (weight * 70),
			LMA = 5
	} else if (weight >= 100) {
		var HRlow = 60,
			HRhi = 100,
			RRlow = 12,
			RRhi = 20,
			SBPlow = 80,
			SBPhi = 120,
			DBPlow = 60,
			DBPhi = 80,
			DefibJ = 200,
			CBVLo = (weight * 65),
			CBVHi = (weight * 70),
			LMA = 6
	};
	// Post-ROSC Ventilation
	if (weight >= 0 && weight <= 5) {
		var DoseVent = "40-60"
	} else if (weight > 5 && weight <= 9) {
		var DoseVent = "30-40"
	} else if (weight > 9 && weight <= 11) {
		var DoseVent = "25-35"
	} else if (weight > 11 && weight <= 18) {
		var DoseVent = "25-30"
	} else if (weight > 18 && weight <= 36) {
		var DoseVent = "20-25"
	} else if (weight > 36 && weight <= 39) {
		var DoseVent = "15-20"
	} else if (weight >= 40) {
		var DoseVent = "10-12"
	};
	// Place observations into the table
	document.getElementById("HRlow").innerHTML = HRlow;
	document.getElementById("HRhi").innerHTML = HRhi;
	document.getElementById("RRlow").innerHTML = RRlow;
	document.getElementById("RRhi").innerHTML = RRhi;
	document.getElementById("SBPlow").innerHTML = SBPlow;
	document.getElementById("SBPhi").innerHTML = SBPhi;
	document.getElementById("DBPlow").innerHTML = DBPlow;
	document.getElementById("DBPhi").innerHTML = DBPhi;
	document.getElementById("DefibJ").innerHTML = DefibJ + " Joules";
	document.getElementById("LMA").innerHTML = "Size " + LMA;
	document.getElementById("CBVLo").innerHTML = CBVLo + " mLs";
	document.getElementById("CBVHi").innerHTML = CBVHi + " mLs";

}

function clearObservations() {
	// Resets "Medication Contraindicated (medcontraid) classes
const idsToClear = ["HRlow", "HRhi", "RRlow", "RRhi", "SBPlow", "SBPhi", "DBPlow", "DBPhi", "DefibJ", "LMA", "CBVLo", "CBVHi"];

for (let i = 0; i < idsToClear.length; i++) {
  document.getElementById(idsToClear[i]).innerHTML = '';
}

	
	};